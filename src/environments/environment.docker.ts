/** URLs cuando el front se sirve en el host (p. ej. http://localhost:8080) y la API en :3000 */
export const environment = {
  production: true,
  baseAPI: 'http://localhost:3000/api',
  wsUrl: 'http://localhost:3000',
};
